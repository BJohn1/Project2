<!DOCTYPE html>
<html lang="en">
<head>
    <title>All Time Starting 5</title>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>Express To-Do</title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
</head> 

<body>
    <h1>All Time Starting 5</h1>
    <ul id='nav' >
        <li class='nav'><a class='nav' href="/users">ALL LINEUPS</a></li>
        
        <li class='nav'>
        </li>
      </ul>
      <% user.lineups.forEach((l,idx) => { %>
        <% l.comment.forEach((c,idx) => { %>
            <% if(c.id===id){ %>
      <form action="/comments/edit/<%=id %>?_method=PUT" method="POST" autocomplete="off">
        <input type="text" name="comment" value="<%= c.comment %>"/>
      <button type="submit">Update Comment</button></form> 
      <%} %>
      <%}) %>
      <%}) %>
</body>
</html>
